import { AddReview as AddReviewComponent } from "@/components/AddReview";

const AddReview = () => {
  return (
    <div className="max-w-2xl mx-auto">
      <AddReviewComponent />
    </div>
  );
};

export default AddReview;
